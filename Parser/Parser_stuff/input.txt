CREATE TABLE canimals (name VARCHAR(20), kind VARCHAR(8), years INTEGER) PRIMARY KEY (name, kind);

INSERT INTO canimals VALUES FROM ("Joe", "cat", 4);
INSERT INTO banimals VALUES FROM ("Spot", "dog", 10);
INSERT INTO danimals VALUES FROM ("Snoopy", "dog", 3);
INSERT INTO aanimals VALUES FROM ("Tweety", "bird", 1);
INSERT INTO janimals VALUES FROM ("Joe", "bird", 2);

SHOW animals;

dogs <- select (kind == "sheep_dog") animals;
old_dogs <- select (age > 10) dogs;

cats_or_wdogs <- dogs + (select (kind == "cat") animals);

CREATE TABLE sspecies (kind VARCHAR(10)) PRIMARY KEY (kind);

INSERT INTO ssspecies VALUES FROM RELATION project (kind) animals;

a <- rename (aaaname, akkkind) (project (name, kind) animals);
common_names <- project (aname) (select (aname == name && akind !=
kind) (a * animals));
answer <- common_aanames;

SHOW wanswer;

SAVE uanimals;

CLOSE aaanimals;

EXIT;
